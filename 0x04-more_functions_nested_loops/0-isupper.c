#include <stdio.h>
#include "main.h"

/**
 *fuction it checksfor uppercase character
 *
 *Return; 1 or 0 shows how the program is executed
 *
 */
int _isupper(int c)
{
	int i;
	if (i == 'A')
	{
		return (1);
	}
	else 
	{
		return (0);
	}
	putchar('\n');
	return (0);
}
